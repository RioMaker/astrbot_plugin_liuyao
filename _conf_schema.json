{
  "default_enabled": {
    "type": "bool",
    "description": "各术数在新群中的默认状态",
    "hint": "建议保持关闭。当前只有六爻；群主或群管理员可使用 /六爻 开 启用。",
    "default": false
  },
  "allow_operator_api_lookup": {
    "type": "bool",
    "description": "缺少原始角色字段时查询群成员资料",
    "hint": "仅调用 OneBot get_group_member_info 读取群主/群管理员身份，不执行管理操作。",
    "default": true
  },
  "max_question_length": {
    "type": "int",
    "description": "问题文本最大长度",
    "hint": "超出部分会被截断，允许范围 20-1000。",
    "default": 200
  },
  "agent_send_chart_image": {
    "type": "bool",
    "description": "Agent 起卦时先发送六爻信息图",
    "hint": "默认开启。图片包含卦象、动爻、起卦人、群号、时间、问题及古籍卦爻辞。",
    "default": true
  },
  "command_send_chart_image": {
    "type": "bool",
    "description": "普通六爻指令发送排盘图",
    "hint": "默认开启。/六爻、/六爻 即时和/六爻 手动都会先发送排盘图，再返回文字解卦。",
    "default": true
  },
  "agent_generate_chart_comment": {
    "type": "bool",
    "description": "Agent 起卦后调用当前会话模型生成图中短评",
    "hint": "默认开启。模型会在成卦后生成简短审慎评语；若用户未给方向，也会根据问题补全方向。",
    "default": true
  },
  "agent_display_name": {
    "type": "string",
    "description": "AI 短评默认署名",
    "hint": "当 Agent 调用工具时未主动传入自身名字，使用此值，例如“可可子”。",
    "default": "AI助手"
  },
  "agent_comment_timeout_seconds": {
    "type": "int",
    "description": "生成图中 AI 短评的超时秒数",
    "hint": "允许范围 5-90；超时或模型失败时自动使用本地保守提示，不影响发图和文字解卦。",
    "default": 45
  },
  "chart_font_path": {
    "type": "string",
    "description": "六爻信息图自定义中文字体路径",
    "hint": "通常留空使用插件内置 Noto Sans CJK；也可填写自定义 .ttf/.ttc/.otf 绝对路径。路径无效时自动回退到内置字体。",
    "default": ""
  },
  "show_disclaimer": {
    "type": "bool",
    "description": "指令结果显示传统文化参考提示",
    "default": true
  }
}



